Use GitHub's public display name for Scholar search instead of the raw username

Semantic Scholar matches by name only. A GitHub handle like 'spearchucker667' will
never match an academic author record, so Scholar was effectively dead code for
outbound-sourced candidates regardless of the deep-tech gate. Now uses the profile's
'name' field when GitHub exposes one, updating the Founder record before the Scholar
lookup runs.
